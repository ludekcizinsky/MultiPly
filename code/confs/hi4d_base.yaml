hydra:
    run:
        dir: "/scratch/izar/cizinsky/multiply-output/hydra/${exp}/${run}"

scene_name: ???

defaults:
  - model: hi4d_model
  - dataset: hi4d
  - _self_

seed: 42
project_name: "MultiPly"
exp: ${dataset.train.dataset}
run: ${dataset.train.subject}

start_frame: 0
end_frame: ???

output_dir: /scratch/izar/cizinsky/multiply-output/training/${scene_name}
pretrained_models_path: /scratch/izar/cizinsky/pretrained
smpl_dir: /home/cizinsky/MultiPly/code/lib/smpl
wandb_dir: /scratch/izar/cizinsky/multiply-output/wandb/

total_num_epochs: 10000
checkpoint_every_n_epochs: 100
check_val_every_n_epochs: 50
